@{
    ViewData["Title"] = "Login";
}

<div class="wrap">
    <h1 class="brand">🏢 CondoGest</h1>
    <p class="sub">Sistema de Gestão de Condomínios</p>

    <div class="card">
        <h2>Login</h2>
        <form asp-action="Login" method="post">
            <label for="email">E-mail</label>
            <input id="email" name="email" type="email" placeholder="seu@email.com" required />

            <label for="senha">Senha</label>
            <input id="senha" name="senha" type="password" placeholder="••••••••" required />

            <button class="btn" type="submit">Entrar</button>

            @if (ViewBag.Mensagem != null)
            {
                <div class="erro">@ViewBag.Mensagem</div>
            }
        </form>
    </div>
</div>

<style>
    body{background:#eef3ff;font-family:Segoe UI,Arial,sans-serif}
    .wrap{max-width:420px;margin:48px auto;text-align:center}
    .brand{color:#2962ff;margin:0}
    .sub{color:#666;margin:6px 0 22px}
    .card{background:#fff;border-radius:14px;padding:24px;
          box-shadow:0 6px 18px rgba(0,0,0,.08);text-align:left}
    .card h2{margin:0 0 14px}
    label{font-size:13px;color:#333}
    input{width:100%;padding:11px;border:1px solid #d9d9d9;border-radius:8px;margin:6px 0 14px}
    .btn{width:100%;padding:12px;border:0;border-radius:8px;background:#2962ff;color:#fff;font-weight:600;cursor:pointer}
    .btn:hover{background:#0f46e6}
    .erro{margin-top:10px;color:#c62828}
</style>
